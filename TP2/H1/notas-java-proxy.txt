
Para correr:
./gradlew clean build
./gradlew bootRun

Para crear y correr el jar:
mvn compile
mvn install
java -jar target/java-proxy-0.0.1.jar

Para correr python:
python clase.py
(NO)flask --app main run


docker build -t python-microservice